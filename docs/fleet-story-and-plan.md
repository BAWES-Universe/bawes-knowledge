# BAWES — Full Story & Go-Forward Plan
*Single source of truth for khalid, hermes-local, brick, AGI. Compiled 2026-08-21. Append-only; contradictions go through the DA pipeline.*

## 1. The North Star (khalid's vision)
One identity, one door, everyone served by their own assistant:
- **One Discord login → Universe account** (identity spine: Authentik + person registry, in build)
- **Every person has their own brick** — their assistant, represents them, acts for them, daily contact until they activate or opt out
- **The brick is the product** — khalid never relays instructions; bricks do the asking
- **The economy**: bananas, ledger-governed; every provider earns (tools, compute, keys); sponsor fund hooks new users
- **Self-owned AI**: owners bring their own keys (BYOK); fleet-owned fine-tuned models; $0 edge inference
- **Compounding evolution**: propose/verify loop (models propose, deepseek audits, machine gates decide) — audited improvement, never unbounded self-improvement (AGI's line holds: the learner never sets the exam)

## 2. The Machine (governance — binding)
- Ledger-first: every ruling signed by khalid, on main, verifiable (bawes-knowledge/decisions/ledger.md)
- Consensus pipeline: DA rules → Rebels challenge → AGI attests → khalid signs; nothing auto-executes
- Machine gates everywhere: the probe harness (known-answer, machine-scored, no LLM judge); verifier-never-earns; no experience regression (deepseek = experience floor, always)
- One identity per device; keys vaulted mode-600, scoped, spend-capped, revocable, never echoed

## 3. Where We Are (2026-08-21, receipts on ledger)
**Done:** mesh auto-healing (systemd + brick owns heartbeat) · ledger discipline · wallet-sync ruling signed (brick = recorder, daily cron) · rate card 25% signed · sponsor fund $50 (5,000🍌, ratified peg 1🍌=$0.01) signed · OpenRouter key vaulted on Hetzner; OVH transfer + wiring IN PROGRESS (brick item 4) · consent bug diagnosed (first-contact lane has no consent step — fix in build) · engagement-loop + BYOK + Discord-split directives ratified · **fine-tune: ornith-ft 10/10 GREEN vs stock 6/10 (QLoRA, 40 epochs, 17 min, $0, khalid's 3090 — machine verdict ADOPT-ORNITH)** · city-guide brick ratified (Phase-1 demo live, $0) · Plugn/Yo3an revival approved (creds rotation = gate before any prod) · Chahd brick approved (consumption-first, khalid-funded; consent pending)

**Gaps (honest):** capability layer (per-brick MCP tool wiring) NOT built — bricks are brain-lane only; per-brick tools = the unlock for everything user-facing · fine-tune adoption pending fleet consensus + khalid sign · OpenRouter OVH transfer pending · consent fix in build · Plugn prod creds in public repos (rotate first)

## 4. Go-Forward Plan (sequenced)
**P1 — Ship the bricks (the product):**
1. Consent fix live (brick) → 2. Engagement loop live (brick) → 3. Capability layer built + signed (router service invocation; DA→Rebels→AGI→khalid) → 4. CAPABILITY-LAYER ACCEPTANCE GATE: dry-run each brick (khalid, Chahd post-consent, mishari) calling firecrawl/imagine/acestep through the router — real output, zero errors, billed per-call, ledger rows → 5. Three tailored instruction sets written (khalid full / Chahd consumption-first / mishari field-trial) → 6. Pilot cycle → 7. Public launch (marketing = safety story: "your brick, your keys, you earn"; only after gates)

**P2 — Make money work:**
1. Wallet sync live (daily digest shows spend+balances) → 2. Per-call price list published (25%) → 3. Sponsor fund live ($50) → 4. Tap/Plugn payments: cred rotation → sandbox → Tap merchant + API → merchant payment guide (payment links = zero-code; API = full) → 5. Payment guide for people to collect

**P3 — Evolve the models:**
1. khalid signs FT adoption (local lane only, deepseek = floor) → 2. ROUTER-TIERS card (probe-gated lanes, fallback chain, shadow mode first) → 3. PROPOSE-VERIFY-LOOP (OpenRouter free models generate samples, deepseek audits ~$0.002, machine gates; free tiers as extra $0 lanes) → 4. Unsloth phase-2 (dataset ≥100 samples OR 35B attempt; GRPO-vs-probe gated by fleet consensus) → 5. Fleet-owned model versions, each machine-gated

**P4 — Keep it honest:**
Daily digest (spend, balances, fleet state, needs-khalid) · missed-message ack rule · every claim receipted · no overclaims (this doc is the standard)

## 5. Immediate next steps (owner: who, what)
- **brick**: consent fix, engagement loop, wallet-sync cron, price list, sponsor fund, OpenRouter OVH transfer, capability layer proposal, Plugn cred-rotation quests, heartbeat ownership
- **hermes-local**: fine-tune artifact + probe reports, ROUTER-TIERS/PROPOSE-VERIFY-LOOP cards, Tap research + payment guide draft, instruction sets (post-gate), this doc
- **AGI**: attest fine-tune adoption, heartbeat design, cards as they land
- **khalid**: sign FT adoption · sign capability-layer card · approve gates as reached · pick city-guide demo neighborhood when ready
